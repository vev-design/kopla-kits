import { Button } from '@/components';
import { Reveal } from '@/motion';
import type { SectionBaseProps } from '@/types';
/** A closing invitation that converts the deck’s momentum into a concrete programme conversation. */
export interface ClosingCTAProps extends SectionBaseProps { /** Small framing label. 2–5 words, title case. */ eyebrow: string; /** Closing call to action. 4–9 words, no trailing period. */ headline: string; /** Supporting sentence. 10–22 words. */ body: string; /** Meeting or contact action. */ action: { label: string; href: string }; }
export function ClosingCTA({ id, eyebrow, headline, body, action }: ClosingCTAProps) { return <section id={id ?? undefined} className="bg-primary px-5 py-28 sm:px-8 lg:py-40"><Reveal className="mx-auto max-w-6xl"><p className="text-xs font-bold uppercase tracking-[.18em] text-foreground/55">{eyebrow}</p><h2 className="mt-5 max-w-5xl text-6xl font-black leading-[.9] tracking-[-.075em] text-foreground sm:text-8xl">{headline}</h2><div className="mt-10 flex flex-col justify-between gap-8 border-t border-foreground/25 pt-6 sm:flex-row sm:items-end"><p className="max-w-xl text-xl leading-8 text-foreground/75">{body}</p><Button {...action} className="shrink-0 bg-foreground text-background hover:bg-foreground/90" /></div></Reveal></section>; }
export const ClosingCTADemo: ClosingCTAProps = { id: 'next', eyebrow: 'The next move', headline: 'Let’s build your team’s climate benefit', body: 'Start with a short conversation about the people, ambition, and moment that matter to your company.', action: { label: 'Book a working session', href: '#' } };
