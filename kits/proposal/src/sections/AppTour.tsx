import { Reveal } from '@/motion';
import type { SectionBaseProps } from '@/types';
/** A spacious, scroll-paced rollout plan that pairs one human image with three practical programme beats. */
export interface AppTourProps extends SectionBaseProps { /** Intro label above the rollout. 3–6 words. */ label: string; /** Editorial image used as the visual anchor. @kind image */ image: string; /** Accessible description for the image. 5–12 words. */ imageAlt: string; /** Image-overlaid programme statement. 6–12 words, no trailing period. */ imageStatement: string; /** Rollout beats. Exactly 3 items, each with a 3–7 word title and 18–35 word description. */ steps: { title: string; description: string }[]; }
export function AppTour({ id, label, image, imageAlt, imageStatement, steps }: AppTourProps) { return <section id={id ?? undefined} className="bg-foreground px-5 py-24 text-background sm:px-8 lg:py-36"><Reveal><p className="text-xs font-bold uppercase tracking-[.18em] text-primary">{label}</p></Reveal><div className="mx-auto mt-12 grid max-w-7xl overflow-clip lg:grid-cols-2"><div className="relative min-h-115 border-b border-background/15 lg:sticky lg:top-0 lg:min-h-screen lg:border-b-0 lg:border-r"><img src={image} alt={imageAlt} className="absolute inset-0 size-full object-cover" /><div aria-hidden className="pointer-events-none absolute inset-0 bg-linear-to-t from-foreground/70 via-transparent to-transparent" /><p className="absolute bottom-8 left-8 max-w-xs text-3xl font-black leading-[.95] tracking-[-.06em]">{imageStatement}</p></div><div className="divide-y divide-background/15 lg:px-14">{steps.map((step, index) => <Reveal key={step.title} className="py-16"><div className="flex gap-5"><span className="text-4xl font-black tracking-[-.08em] text-primary">0{index + 1}</span><div><h2 className="text-4xl font-black leading-[.94] tracking-[-.06em] sm:text-5xl">{step.title}</h2><p className="mt-6 max-w-md text-lg leading-8 text-background/65">{step.description}</p></div></div></Reveal>)}</div></div></section>; }
export const AppTourDemo: AppTourProps = { id: 'tour', label: 'A simple rollout', image: 'https://images.unsplash.com/photo-1491438590914-bc09f6a6b5a8?auto=format&fit=crop&w=1200&q=85', imageAlt: 'Colleagues walking together through a green park', imageStatement: 'A programme with room for every kind of action', steps: [{ title: 'Open with a shared moment', description: 'Launch around a moment that already has meaning for your people, so the programme arrives as an invitation rather than another policy.' }, { title: 'Make participation easy', description: 'Give each teammate a clear first action, practical learning, and access to climate projects they can understand and support.' }, { title: 'Keep the story moving', description: 'Share progress in small, regular beats that make the team’s collective participation visible and worth continuing.' }] };
