import { Reveal, Stagger } from '@/motion';
import type { SectionBaseProps } from '@/types';
/** A calm three-part explanation of a team climate programme, from a compelling employee benefit to participation that lasts. */
export interface StrategyProps extends SectionBaseProps { /** Small framing label. 2–5 words, title case. */ eyebrow: string; /** Centered section title. 4–8 words, no trailing period. */ headline: string; /** Supporting copy. 1–2 sentences, 16–32 words. */ body: string; /** Programme pillars. Exactly 3 items, each with a 1–2 word title and 14–22 word description. */ items: { title: string; description: string; symbol: string }[]; }
export function Strategy({ id, eyebrow, headline, body, items }: StrategyProps) { return <section id={id ?? undefined} className="bg-background px-5 py-24 sm:px-8 lg:py-36"><Reveal><div className="mx-auto max-w-3xl"><p className="text-xs font-bold uppercase tracking-[.18em] text-muted-foreground">{eyebrow}</p><h2 className="mt-5 text-5xl font-black leading-[.9] tracking-[-.075em] sm:text-7xl">{headline}</h2><p className="mt-7 max-w-2xl text-lg leading-8 text-muted-foreground">{body}</p></div></Reveal><Stagger className="mx-auto mt-14 grid max-w-7xl gap-px bg-border md:grid-cols-3">{items.map((item, index) => <article key={item.title} className="soft-transition bg-card px-8 py-12 hover:bg-primary"><span className="text-sm font-black tracking-[.08em] text-muted-foreground">0{index + 1}</span><h3 className="mt-16 text-4xl font-black leading-none tracking-[-.06em]">{item.title}</h3><p className="mt-5 max-w-xs leading-7 text-muted-foreground">{item.description}</p></article>)}</Stagger></section>; }
export const StrategyDemo: StrategyProps = { id: 'strategy', eyebrow: 'Why this works', headline: 'A benefit people can participate in', body: 'Greenline makes climate action part of the way people show up at work: tangible, inclusive, and easy to keep moving.', items: [{ title: 'Meaningful', description: 'A climate benefit connects the company’s values to something each teammate can understand and act on.', symbol: '01' }, { title: 'Personal', description: 'Individual pathways make room for different lifestyles, starting points, and forms of climate action.', symbol: '02' }, { title: 'Collective', description: 'Shared progress turns isolated good intentions into a visible culture of care and momentum.', symbol: '03' }] };
