import { cn } from '@/lib/utils';
import { Button } from './Button';
/** A compact responsive brand header with in-page navigation and a single action. */
export interface NavProps { /** Wordmark displayed at the start of the bar. 1 word, lowercase preferred. */ brand?: string; /** Navigation links. 3–5 short labels with in-page destinations. */ links?: { label: string; href: string }[]; /** Optional action displayed at the end of the desktop header. */ action?: { label: string; href: string } | null; /** Header treatment. */ variant?: 'default' | 'floating'; }
export function Nav({ brand = 'Greenline', links = [], action = null, variant = 'default' }: NavProps) { return <header className={cn('relative z-20 w-full border-b border-border/45 bg-background transition-[background-color,border-color] duration-200', variant === 'floating' && 'mx-auto max-w-6xl rounded-b-3xl border-x')}><nav aria-label="Primary" className="mx-auto flex min-h-19 max-w-6xl items-center justify-between gap-4 px-5 sm:px-8"><a href="#top" className="flex shrink-0 items-center gap-2 text-2xl font-black tracking-[-.08em] no-underline hover:no-underline"><span className="h-4 w-4 rounded-sm bg-primary [clip-path:polygon(0_0,100%_0,100%_100%,36%_75%)]" />{brand}</a><div className="hidden items-center gap-9 text-sm font-bold lg:flex">{links.map((link) => <a key={link.href} href={link.href} className="text-foreground no-underline">{link.label}</a>)}</div><div className="hidden sm:block">{action && <Button {...action} variant="outline" size="sm" />}</div><details className="relative lg:hidden"><summary className="list-none rounded-xl border border-border px-3 py-2 text-sm font-bold [&::-webkit-details-marker]:hidden">Menu</summary><div className="absolute right-0 mt-2 grid min-w-44 gap-1 rounded-2xl border border-border bg-background p-3 shadow-lg">{links.map((link) => <a key={link.href} href={link.href} className="rounded-lg px-2 py-2 text-sm font-bold no-underline hover:bg-muted">{link.label}</a>)}</div></details></nav></header>; }
export const NavShowcase = [{ label: 'Standard navigation', props: { brand: 'Greenline', links: [{ label: 'About', href: '#about' }, { label: 'Projects', href: '#projects' }, { label: 'Blog', href: '#blog' }], action: { label: 'Get the app', href: '#' }, variant: 'default' } }];
