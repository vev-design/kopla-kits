import { Button } from '@/components';
import { MediaBlock, type ImageBlockProps, type VideoBlockProps } from '@/components/blocks';
import type { SectionBaseProps } from '@/types';
/** Split product scene with a media frame and native disclosure details. Use to explain a foundational capability without turning the presentation into dense documentation. */
export interface GraphFeatureProps extends SectionBaseProps { /** Intro label. 1–3 uppercase words. */ eyebrow: string; /** Section title. 4–8 words, no trailing period. */ headline: string; /** Content-media frame. */ media: ImageBlockProps | VideoBlockProps; /** Capability disclosures. 3–5 items; each title 1–4 words and body 1–2 sentences. */ items: { title: string; body: string }[]; /** Follow-up action. */ cta?: { label: string; href: string } | null; }
export function GraphFeature({ id, eyebrow, headline, media, items, cta = null }: GraphFeatureProps) { return <section id={id ?? undefined} className="mx-auto max-w-[1400px] px-5 py-24 lg:px-8 lg:py-36"><p className="text-sm font-[550] uppercase tracking-[.08em]">{eyebrow}</p><h2 className="mt-4 max-w-2xl text-5xl font-[350] leading-none tracking-[-.065em] lg:text-7xl">{headline}</h2><div className="mt-16 grid gap-12 lg:grid-cols-[1.08fr_.92fr]"><div className="overflow-clip rounded-2xl bg-primary p-3 [&_img]:h-full [&_img]:min-h-[420px] [&_img]:w-full [&_img]:rounded-xl [&_img]:object-cover [&_video]:h-full [&_video]:min-h-[420px] [&_video]:w-full [&_video]:rounded-xl [&_video]:object-cover"><MediaBlock media={media}/></div><div className="self-center">{items.map((item, index) => <details key={item.title} name="graph-capability" open={index === 0} className="border-t py-6 last:border-b"><summary className="flex cursor-pointer list-none items-center justify-between text-2xl font-[500] tracking-[-.04em] [&::-webkit-details-marker]:hidden">{item.title}<span className="text-muted-foreground">+</span></summary><p className="max-w-xl pt-5 text-lg leading-relaxed text-muted-foreground">{item.body}</p></details>)}{cta && <Button href={cta.href} variant="outline" className="mt-8">{cta.label}</Button>}</div></div></section>; }
export const GraphFeatureDemo: GraphFeatureProps = { id: 'work-graph', eyebrow: 'The work graph', headline: 'AI that works the way your team works', media: { kind: 'image', src: 'https://images.unsplash.com/photo-1551288049-bebda4e38f71?auto=format&fit=crop&w=1400&q=85', alt: 'Connected work graph interface' }, items: [{ title: 'Work Graph', body: 'A connected view of people, work, and dependencies so every decision has the right context.' }, { title: 'Multiplayer', body: 'People and agents work together in the same flow of work.' }, { title: 'Shared memory', body: 'Important context stays accessible to every teammate.' }, { title: 'Enterprise governance', body: 'Control is built into the work, not added afterward.' }], cta: { label: 'Learn more', href: '#agents' } };
