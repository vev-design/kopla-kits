import { Icon } from '@/components/Icon';
import type { SectionBaseProps } from '@/types';
const usedIcons = ['arrow-right', 'trophy'] as const;
/** Recognition cards that turn third-party proof into a clean editorial beat. Use just before the closing call to action to reinforce platform confidence. */
export interface AwardsProps extends SectionBaseProps { /** Section title. 3–6 words, no trailing period. */ headline: string; /** Recognition cards. Exactly 2–3 items. */ items: { title: string; description: string; href: string; variant: 'dark' | 'green' }[]; }
export function Awards({ id, headline, items }: AwardsProps) { return <section id={id ?? undefined} className="mx-auto max-w-[1400px] px-5 py-24 lg:px-8 lg:py-36"><h2 className="text-5xl font-[350] tracking-[-.065em] lg:text-7xl">{headline}</h2><div className="mt-12 grid gap-8 md:grid-cols-2">{items.map((item) => <article key={item.title}><div className={item.variant === 'dark' ? 'flex aspect-[1.6] flex-col justify-between rounded-2xl bg-primary p-8 text-primary-foreground' : 'flex aspect-[1.6] flex-col justify-between rounded-2xl bg-[oklch(from_#005843_l_c_h)] p-8 text-primary-foreground'}><Icon name="trophy" size={36}/><p className="max-w-sm text-3xl font-[350] leading-none tracking-[-.05em]">{item.title}</p></div><p className="mt-6 max-w-xl text-lg leading-relaxed">{item.description}</p><a href={item.href} className="mt-5 inline-flex items-center gap-2 border-b-2 border-transparent font-[500] hover:border-[oklch(from_#cd4848_l_c_h)] hover:text-[oklch(from_#cd4848_l_c_h)]">Learn more <Icon name="arrow-right" size={16}/></a></article>)}</div></section>; }
export const AwardsDemo: AwardsProps = { id: 'awards', headline: 'Recognized as a leader', items: [{ title: 'A leader in collaborative work management', description: 'Recognized for three consecutive years by leading industry analysts.', href: '#', variant: 'dark' }, { title: 'Top-rated work management software', description: 'Thousands of customer reviews recognize a platform teams enjoy using.', href: '#', variant: 'green' }] };
