import type { ButtonHTMLAttributes, ReactNode } from 'react';
import { cva } from 'class-variance-authority';
import { cn } from '@/lib/utils';
const styles = cva('asana-action inline-flex items-center justify-center gap-2 rounded-full border px-5 py-3 text-sm font-[500] leading-none', { variants: { variant: { solid: 'border-primary bg-primary text-primary-foreground hover:bg-[oklch(from_#f06a6a_l_c_h)] hover:text-primary', outline: 'border-primary bg-background text-primary hover:bg-secondary', light: 'border-transparent bg-background text-primary hover:bg-secondary', dark: 'border-transparent bg-primary text-primary-foreground hover:bg-[oklch(from_#3d3d3d_l_c_h)]', gradient: 'border-transparent bg-[linear-gradient(90deg,oklch(from_#f7a1bc_l_c_h),oklch(from_#f5b4ed_l_c_h))] text-primary hover:brightness-95' }, size: { sm: 'px-4 py-2.5 text-xs', md: 'px-5 py-3 text-sm', lg: 'px-7 py-4 text-base' } }, defaultVariants: { variant: 'solid', size: 'md' } });
/** Pill-shaped action used for primary, secondary, and dark-surface calls to action. */
export interface ButtonProps extends ButtonHTMLAttributes<HTMLButtonElement> { /** Visible action label. 1–4 words, sentence case. */ children?: ReactNode; /** Visual emphasis. */ variant?: 'solid' | 'outline' | 'light' | 'dark' | 'gradient'; /** Button scale. */ size?: 'sm' | 'md' | 'lg'; /** Optional destination; renders an anchor when supplied. @kind url */ href?: string; }
export function Button({ children = 'Get started', variant = 'solid', size = 'md', href, className, ...props }: ButtonProps) { const classNames = cn(styles({ variant, size }), className); return href ? <a href={href} className={classNames}>{children}</a> : <button className={classNames} {...props}>{children}</button>; }
export const ButtonShowcase = [{ label: 'Primary', props: { children: 'Get started', variant: 'solid', size: 'md' } }, { label: 'Outline', props: { children: 'Contact sales', variant: 'outline', size: 'md' } }, { label: 'Dark', props: { children: 'Learn more', variant: 'dark', size: 'md' } }];
