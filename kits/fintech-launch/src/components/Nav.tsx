import { cn } from '@/lib/utils';
import { Button } from './Button';
/** A responsive navigation bar with a wordmark, in-page links, and a primary action. */
export interface NavProps { /** Text wordmark. 1 word. */ brand?: string; /** Navigation links. 3–7 items, each with a short label and URL. */ links: { label: string; /** @kind url */ href: string }[]; /** Primary navigation action. */ action?: { label: string; /** @kind url */ href: string } | null; /** Surface contrast. */ variant?: 'light' | 'dark'; /** Optional extra classes. */ className?: string; }
export function Nav({ brand = 'Qonto', links, action, variant = 'dark', className }: NavProps) { const dark = variant === 'dark'; return <header className={cn('flex items-center justify-between gap-4 px-5 py-4 transition-colors duration-500 ease-out sm:px-8', dark ? 'bg-[var(--dark)] text-primary-foreground' : 'bg-background text-foreground', className)}><a href="#top" className="shrink-0 text-[27px] font-semibold tracking-[-0.01em]">{brand}</a><nav className="hidden min-w-0 items-center gap-5 lg:flex" aria-label="Primary">{links.map((link) => <a key={link.href} href={link.href} className="qonto-underlined whitespace-nowrap text-sm opacity-75 hover:opacity-100">{link.label}</a>)}</nav><div className="flex shrink-0 items-center gap-3"><span className="hidden text-sm opacity-60 sm:inline">EN</span>{action ? <Button {...action} size="sm" variant={dark ? 'light' : 'solid'} /> : null}</div></header>; }
export const NavShowcase = [{ props: { brand: 'Qonto', links: [{ label: 'Business account', href: '#account' }, { label: 'Financial tools', href: '#tools' }, { label: 'Pricing', href: '#pricing' }], action: { label: 'Open an account', href: '#' }, variant: 'dark' } }];
