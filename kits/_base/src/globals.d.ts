// Ambient type declarations. This file MUST NOT contain any
// import/export statements — that would turn it into a module and the
// ambient module declarations below would no longer apply globally.

// Plain CSS — side-effect imports (e.g. `import './globals.css'`).
declare module '*.css';
