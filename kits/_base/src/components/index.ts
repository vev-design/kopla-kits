// Barrel for shadcn-style UI primitives. Add re-exports here as you run
// `npm run ui:add <component>` (or `npx shadcn@latest add <component>`).
// Sections import from `@/components/ui/...` directly; this barrel exists
// for callers that want a single import surface.
export * from './ui/button';
